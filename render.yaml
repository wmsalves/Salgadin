services:
  - type: web
    name: salgadin-api
    runtime: docker
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: PORT
        value: 10000
      - key: SUPABASE_DB_CONNECTION
        sync: false
      - key: Jwt__Key
        sync: false
